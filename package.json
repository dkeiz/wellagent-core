{
  "name": "wellagent-core",
  "version": "0.3.0",
  "description": "Core runtime engine for LocalAgent.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LocalAgent",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^9.0.1",
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2"
  },
  "devDependencies": {}
}
