# Telegram relay plugin

Install this plugin's independent runtime when Telegram support is needed:

    npm install --omit=dev --prefix agentin/plugins/telegram-relay

Telegram packages are resolved from this plugin folder and are not core
LocalAgent dependencies.
